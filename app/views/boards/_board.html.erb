<div class="col-sm-12 col-lg-4 mb-3">
              <div id="board-id-<%= board.id %>">
                <div class="shop-card">
                  <%= image_tag "shop-image.png", class: "shop-img-top", width: "200", height:"200" %>
                  <div class="shop-body">
                    <div class="d-flex">
                      <h4 class="shop-title">
                        <%= link_to board.title, '#' %>
                      </h4>
                      <div class='ms-auto'>
                        <%= link_to '#', id: "button-edit-#{board.id}" do %>
                          <i class="bi bi-pencil-fill"></i>
                        <% end %>
                        <%= link_to '#', id: "button-delete-#{board.id}", method: :delete, data: {confirm: ''} do %>
                          <i class="bi bi-trash-fill"></i>
                        <% end %>
                      </div>
                    </div>
                    <ul class="shop-list-inline">
                      <li><i class="bi bi-person"></i><%= board.user.decorate.full_name %></li>
                      <li><i class="bi bi-calendar"></i><%= l board.created_at, format: :short %></li>
                    </ul>
                    <p class="shop-text">ここがおすすめ！</p>
                  </div>
                </div>
              </div>
            </div>